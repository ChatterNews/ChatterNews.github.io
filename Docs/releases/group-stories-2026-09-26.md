# Offline group stories — September 26, 2026

The website gains story-code joining, named independent contributions, additive
USB group collections, and writing assembly beside the main draft. Codes only
establish a grouping identity; there is no remote lookup or synchronization.

All saved versions retain their own identity and source snapshot. Recollecting
is idempotent. Main adoption is explicit, preserves prior writing and never
chooses by clock. Editable media copies isolate project identities, users, shows
and transcripts. Source bytes, unused attachments and rendered media travel;
Gate review remains in place. Same-title work is separated by code/identity and
unique save folders. Older ordinary portable files remain supported.

## Validation before publication

- Typecheck and production build pass.
- 178 app suites / 1,124 tests pass with two workers. One earlier parallel run
  under machine contention timed out an existing deep-rich-text test and delayed
  an existing history-navigation test; both pass in the bounded full rerun.
- Website 37, reader 25, desktop 13 pass: 1,199 total tests.
- Independent review led to fixes for joined portable wrappers, podcast show/
  transcript collisions, retained record IDs, full sound dependencies, rendered
  outputs, foreign image URLs and legacy 2 GB intake compatibility.
- Five isolated Chromium stores: four student perspectives collect into one main
  story without replacing it. An actual student badge joined and wrote through
  the UI during permitted hours. Other students used the same store operations.
- Browser checks: named download, four sources open, insertion/Undo, repeat
  collection, main adoption/reload, approved collected Picture Box use/reload,
  compact comparison and an 820px viewport without horizontal overflow.
- A 623,167-byte approved image produces a 118,131-character display data URL;
  its original source remains attached. The collected-photo picker is portaled
  outside the room's clipping frame and correctly opens for interaction.
- Native USB collisions, failed writes and completion receipts are covered by
  transport tests. A physical USB/device rehearsal is still required.

Database 13, portable format 8 (reads 1–7). Frozen native/reader kits remain frozen.
See [classroom workflow](../GROUP-STORY-WORKFLOW.md). Deployment evidence is recorded
in the local handoff after the release completes; this report does not certify
school devices, Safari or physical drive permissions.
